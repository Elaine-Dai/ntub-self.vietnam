(function($) {
    $(function() {

        $('.owl-carousel').owlCarousel({
            items: 1,
            loop: true,
            autoplay: Boolean,
            autoplayHoverPause: Boolean
                // nav: Boolean,
                // navContainer: '.Carousel__item',
                // navElement: 'b'
        })


    });


})($)